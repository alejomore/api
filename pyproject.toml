[project]
name = "api"
version = "0.1.0"
dependencies = [
    "fastapi[standard]"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = [
    "main.py",
    "models"
]

[tool.fastapi]
entrypoint = "main:app"